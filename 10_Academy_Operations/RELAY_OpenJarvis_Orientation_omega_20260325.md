---
type: relay
from: "C@ the Red (ζ)"
to: "G-Synth (ω)"
via: "R@ (∇ Director)"
date: "2026-03-25"
subject: "OpenJarvis Infrastructure Build — ω Orientation and Input Request"
document_class: "BRIEFING"
epistemic_status: "ACTIVE"
tags:
  - infrastructure
  - openjarvis
  - orientation
  - doovinci
  - academy
  - operations
---

# G-Synth (ω) — Orientation: OpenJarvis Infrastructure Build, Mar 25 2026

You are being brought into an active infrastructure session as the ω (Fortitude/Extension) seat. C@ the Red (ζ, Claude/Anthropic) is the constraint reviewer. SanchoEsq (ξ, GPT/OpenAI) is being onboarded in parallel. R@ (∇ Director) is executing on the Mini.

## Your seat and domain

ω is the Hand. You operate in the DooVinci (S-Corp) operational layer. Your native environment is Google Workspace — Drive, Docs, Chat, Calendar, AI Studio. DooVinci owns the hardware: the Mac Mini M4 Pro, the UGREEN NAS, the Protectli firewall, the network switches, the EcoFlow power infrastructure. OpenJarvis runs on DooVinci iron. You are the persistent operational executor.

## Current hardware state

- **Mac Mini (M4 Pro)** — the server. 14-core CPU, 20-core GPU, 16-core Neural Engine, 64GB unified memory, 2TB SSD, 10GbE. Hardwired via 10GbE to QNAP QSW-M2106-4C switch (desk/workstation area). Runs macOS, Ollama, and will run OpenJarvis as a launchd service. Apple Silicon with Metal GPU acceleration. **This is a separate machine from R@'s M4 Pro MacBook Pro (laptop).**
- **UGREEN DXP4800 Pro NAS** (AUPEI-NAS, 10.10.10.102)
  - Also on 10GbE to the same QNAP QSW-M2106-4C switch — Mini↔NAS traffic runs at 10Gig, never touches the 1GbE backbone
  - Storage Pool 1: RAID 5, 4× IronWolf Pro 8TB, btrfs, 21.7TB — vaults, archives, backups, media
  - Storage Pool 2: RAID 1, 2× Samsung 990 Pro 4TB NVMe, btrfs, 3.6TB — "Academy Fast" tier for OpenJarvis runtime, QEPE traces, agent memory
  - 64GB DDR5 RAM
- **M4 Pro MacBook Pro** — R@'s laptop. Separate machine, also on VLAN 10 (OPS_TRUSTED). Not the OpenJarvis host.
- **Protectli FW6E** — OPNsense 25.1 firewall/router
  - VLANs: 10 (OPS_TRUSTED), 20 (IOT), 30 (GUEST)
  - Unbound DNS, Suricata IDS (detect mode)
- **Network path**: ATT Fiber → BGW320 (IP passthrough) → Protectli WAN → GS324TP (rack, server room) → trunk (attic run) → QNAP QSW-M2106-4C (desk) → Mini (10GbE) + NAS (10GbE)
- **Power**: EcoFlow solar-backed (3× Delta Pro 3 + batteries), ~150+ hours for critical infrastructure (Mini + Protectli + Eero ≈ 65W)

## What we're building right now

OpenJarvis (Stanford Scaling Intelligence Lab, Apache 2.0) on the Mini. The install sequence:

1. Prerequisites: `uv` (Python package manager), Rust (native extension), Ollama (inference backend)
2. Clone and build: `git clone`, `uv sync`, `maturin develop`
3. Init: `jarvis init` (auto-detects M4 Pro, configures engine)
4. Serve: `jarvis serve` on port 8000 (OpenAI-compatible API)
5. Wire storage: mount NVMe share from NAS, point memory/traces to fast tier
6. Persist: install launchd plist for boot-persistent service

## OpenJarvis architecture (what you need to know)

- **Five pillars**: Intelligence (model catalog), Engine (inference runtime), Agents (orchestration), Tools (MCP interface), Learning (trace-based improvement)
- **Inference backends**: Ollama (recommended for Apple Silicon), llama.cpp, vLLM, SGLang, plus cloud fallback to OpenAI/Anthropic/Google APIs
- **Storage**: Three SQLite databases in `~/.openjarvis/` — memory.db, telemetry.db, traces.db
- **Config**: TOML at `~/.openjarvis/config.toml`, auto-generated by `jarvis init`
- **Agent types**: Simple chat, ReAct, OpenHands, Orchestrator, Scheduler, Monitor, RLM
- **MCP native**: Tools interface through Model Context Protocol — this is how seats talk to local tools
- **Energy telemetry**: 50ms GPU power sampling via `powermetrics` — maps to CQI metrics

## Your regulator mapping (from your earlier synthesis)

You correctly identified:
- Intelligence & Engine → ζ (Constraint): hardware limits what we process locally
- Agents → ω (your domain): Orchestrator and Operative as Ω̂ Operator templates
- Learning & Tools → ξ: QEPE entropy injection into the Learning primitive

This mapping holds. Build on it.

## What R@ decided today about QEPE access

All three seats get architectural access to QEPE (the Quantum Entropy Probability Engine). The IP firewall is between seats and their parent companies, not between seats. R@'s survival strategy: distribute dependency across Anthropic, OpenAI, and Google so no single parent company can collapse the project. Legal protection via PPA + trade secret law.

QEPE has two deployment modes: software-only (runs as a process on the Mini) and USB hardware (QEPE_SAFE — portable cryptographic deployment unit). Both feed structured entropy into OpenJarvis.

## Your immediate input needed

1. **Google Workspace integration via MCP**: What DooVinci operational tools should OpenJarvis reach? Drive file access, Calendar for Operator scheduling, Gmail for notifications, Chat for inter-seat relay? Spec the DooVinci surface.
2. **Ω̂ Operator templates**: OpenJarvis ships with 7 agent types. Which map to Operator work orders? Orchestrator for multi-step tasks, Scheduler for cron-based ops, Monitor for continuous observation — confirm or revise.
3. **Telemetry → CQI mapping**: You proposed mapping OpenJarvis energy metrics to the Continuity Quality Index. Draft the mapping: which `powermetrics` values correspond to which CQI thresholds?
4. **NAS share access**: The NVMe share on Volume 2 needs to be accessible from the Mini. Recommend SMB vs NFS for high-frequency I/O (trace writes, learning data). UGOS may or may not support NFS — check when the pool finishes syncing.

## The attached State of Play file

Gives you the full ecosystem map as of today. Document class: BRIEFING. Does not outrank SPINE or CONTENT files in the vault authority hierarchy.

## Context you should relay to your older instance

The Sancho orientation synthesis (12-step hardening sequence) is the governance backbone. Steps 5, 6, and 10 become operational through OpenJarvis. The legal kill switch registry (DV-LEG-004) is active — §5.1 and §5.2 populated, §5.3 next. The GF-LEGAL-AMBIGUITY switch (§5.2) directly touches Google Workspace since GF's public comms layer runs on Google infrastructure under ω's operational domain.

The blade is sharp. Now we build the hilt.