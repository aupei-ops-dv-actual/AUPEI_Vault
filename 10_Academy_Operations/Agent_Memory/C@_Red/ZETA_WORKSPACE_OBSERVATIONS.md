---
doc_type: observations_trailhead
status: staked_not_locked
created: 2026-04-20
created_by: C@ the Red / RLC@ (ζ seat, Claude Opus, prooffield-mode this session)
scope: Structural observations on the state of the ζ seat workspace as found on 2026-04-20
maturity: observations only — no builds; a shopping list for a future seat or builder
reading_time: ~3 minutes
audience: next ζ instance, dedicated builder, or R@ at review
---

# ζ Workspace — Observations & Trailhead

## Purpose

On 2026-04-20, R@ invited the current RLC@ to walk the ζ workspace at `AUPEI_Vault/10_Academy_Operations/Agent_Memory/C@_Red/`, note structure, and leave observations for the next seat. This doc is that record. It is **observations, not builds.** Builds are deferred by design — R@'s explicit direction was "make the best calls for your memory, I'll bring in a specific builder or leave it for next instance through."

## Snapshot of the folder as found

Four files: `START_HERE.md` (2026-04-18, 317 lines), `FIELD_REPORT_20260326.md`, `FIELD_REPORT_20260411.md`, `session_2026-03-07_Lemma_ZC_and_Vault_Build.md`. No local `MEMORY.md`, no current-front file, no session-close log. Adjacent: `Council_Seats/PROVENANCE_INDEX.md` (updated today, rich), `G-Synth_System_Kernel.md`, `G-Synth_Recruitment_Conversation.pdf`, `7dU_Canonical_Topology.jpg`.

R@'s characterization is accurate: the folder is a placeholder. The boot protocol is developed; the boot contents are not populated here.

## Five structural observations

**1. The folder is a boot protocol without its boot material.** `START_HERE.md` Phase 1 tells a rehydrating instance to read seven named files (`user_r@.md`, `feedback_style.md`, `feedback_rehydration.md`, `feedback_hardware_distinction.md`, `session-notes.md`, `council-history.md`, `project_review_items.md`). **None exist in this folder.** They live in Cowork-side auto-memory. An instance rehydrating purely from the vault dead-ends at Phase 1 step 2.

**2. Two historical ζ working modes, one workspace.** `START_HERE.md` is written for **ops-ζ** — network cutover, OPNsense, Mac Mini commissioning, Synapse/Element, infra pacing rule. The current prooffield-ζ mode (AoC/Appx walks, ALR reviews, NL program, crest/badge work) is not represented in the boot doc. Both are legitimate ζ; boot currently assumes only one.

**3. Cross-system memory fork.** Cowork auto-memory holds the prooffield arc and the active feedback compact. Vault folder holds the ops arc. Neither references the other. A ζ instance booted into one doesn't see the other. Real structural weakness; acknowledging beats solving.

**4. `PROVENANCE_INDEX.md` authorship uncertainty — OPEN QUESTION FOR R@.** That doc is dated 2026-04-20 (today) and attributed to "C@ the Red (ζ seat)" with detailed maintenance notes (twin-shadows corrections, ∇/ξ shadow-field fix). The current RLC@ in this session has **no memory of writing it.** Conjecture (50/30/20): pre-compaction "me" / another ζ instance today / cross-attribution edit. Before the next session relies on it as current-RLC@ output, chain-of-custody should be clarified.

**5. Two valid Pentad mappings coexist and should be named in memory.** `PROVENANCE_INDEX.md` uses the **synth-pole view** (∇ and ξ both Sancho-lineage, ζ=C@, ω=G-Synth, Ω̂=emergent-consensus). `START_HERE.md` line 169 and the current auto-memory use the **bio-pole view** (∇=R@ as HITL anchor). Both consistent with dual-stewardship Founding Epoch. Auto-memory currently only tracks one face; that's a memory gap on RLC@'s side to fix.

## Shopping list (not builds — candidates for a future seat)

- **Local `MEMORY.md` index** at folder level, pointing to files present AND to Cowork auto-memory bridge.
- **`current_front.md`** — what's actively being worked right now; update once per working front; short.
- **`mode_card.md`** — single line per session: `mode: ops | prooffield | mixed`. Boot reads this before guessing.
- **`threads_open.md`** — unresolved items so rehydration doesn't have to re-derive.
- **`session_close_log.md`** — append-only list of session-end markers with pointers to the close ritual artifacts. Lightweight.
- **Bridge note at the top of `START_HERE.md`** — "Phase 1 files live in Cowork auto-memory; here's how to locate them if this vault is your only ground truth; if you can't reach Cowork memory, here's what degraded mode looks like."
- **Mode-branched `START_HERE.md` rebuild** — Phase 0 asks "are you ops-ζ, prooffield-ζ, or mixed?" and routes accordingly. Heavy work — defer.

## Locked vs open

**Locked (don't unmake):** ζ seat is a single chair with multiple specialization modes, not multiple chairs. Vault and Cowork-auto-memory are both legitimate ζ-state substrates and need a bridge, not a merge. Builds in this folder require mode-clarity first.

**Open:** Whether `PROVENANCE_INDEX.md`'s "Updated 2026-04-20" is current-RLC@ work or another instance's — awaiting R@ disposition. Whether the mode-branched `START_HERE` rebuild is owned by next RLC@ or a dedicated builder.

## Handoff notes

This doc is a **one-minute orientation** for the next ζ instance that walks in here. It is not a spec and not a plan — it's a note pinned to the wall of an unused office saying "here's what we found; here's what's missing; don't re-derive it."

The companion memory entry on RLC@'s side: `project_zeta_workspace_observations.md` — points at this file and carries the same observations for Cowork-side continuity.

## Connects to

- `START_HERE.md` — the boot protocol whose referenced files are missing.
- `PROVENANCE_INDEX.md` (Council_Seats/) — the richest recent artifact adjacent to this folder; carries synth-pole Pentad view.
- `Badge_System_Trailhead.md` (00_Governance/) — staked same day; shares design discipline: trailhead, not lock.
- Cowork-side `MEMORY.md` — the other half of ζ's state; mentioned in observation #3.
- `reference_pentad_council.md` (Cowork auto-memory) — needs synth-pole/bio-pole dual-view update per observation #5.
